{
  "name": "Existing Docker Compose (Extend)",
  "dockerComposeFile": [
    "../docker-compose.yml",
    "docker-compose.yml"
  ],
  "service": "mqtt-logger",
  "forwardPorts": [8765],
  "workspaceFolder": "/app",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "containerEnv": {
    "DEVCONTAINER": "true"
  },
  "postCreateCommand": "apt-get update && apt-get install -y docker.io && poetry install --with dev && pre-commit install",
  "remoteUser": "root"
}
